/*!
 * \page ThemeManagement Theme management selenium tests
 *
 * \section SecNewThm New theme
 *  Test for adding a new theme to the application.
 *  It creates a new 'Theme test' theme. This are the parameters given to the new theme:
 *    - Name : 'Theme test'
 *
 * \section SecThmViw View theme
 *  Test for verifying that the added theme is correct.
 *  This process search the name of the new theme in the correspondent criterion
 *  of theme management window, it selects it and goes to 'View' window to visualise it.
 *
 * \section SecChangeThm Update theme
 *  Test for modifying a defined theme.
 *  This process search the name of the new theme in the correspondent criterion
 *  of theme management window, it selects it and goes to 'Update' window to modify it.
 *  Afterwards, it changes the theme to which it is assigned setting name to 'Theme changed' 
 *  and confirmes the modifications.
 *
 * \section SecViwChangeThm View updated theme
 *  Test for verifying that the modifications made on the theme are correct.
 *  This process search the name of the updated theme in the correspondent criterion
 *  of theme management window, it selects it and goes to 'View' window to to visualise it.
 *
 * \section SecThmDel Delete theme
 *  Test for verifying the delete of one theme.
 *  This process search the name of the 'Theme changed' theme in the correspondent criterion
 *  of theme management window, it selects it and pushes 'Delete' button to delete it.
 *
 * \section SecViwDelThm Verify Delete theme
 *  Test for verifying that the delete of theme 'Theme changed' was correct.
 */
